from utils.processor import Processor

if __name__ == "__main__":
    processor = Processor("./data")
    # processor.regression('mouse')
    # processor.regression('touchpad')
    # processor.showScatterGraph()
